
using UnityEngine;
using UnityEngine.UI;


public class OperatorInfoPanel : MonoBehaviour
{
    private Text nameText;
    private Text statsText;

    public void UpdateInfo(OperatorData operatorData)
    {
        //nameText.text = operatorData.operatorName;
        //statsText.text = $"HP : {operatorData.baseStats.Health}\nATK : {operatorData.baseStats.AttackPower}";
    }
}
