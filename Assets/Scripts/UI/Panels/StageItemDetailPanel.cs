using UnityEngine;

// ItemUIElement를 클릭했을 때 나타날 패널
public class ItemDetailPanel : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
